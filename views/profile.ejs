<!doctype html>
<html>
<head>
    <title>Vent</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/bootstrap/3.0.2/css/bootstrap.min.css">
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.0.3/css/font-awesome.min.css">
    <link href="https://fonts.googleapis.com/css?family=Oswald&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="style.css">
      <script src="https://kit.fontawesome.com/077191d827.js" crossorigin="anonymous"></script>
</head>
<body>
<div class="container">
    <div class="page-header text-center">
        <!-- <h1><span class="fa fa-anchor"></span>Stay Healthy Profile Page</h1> -->
        <h1>Got A Lot On Your Mind? Vent.</h1>
        <a href="/logout" class="btn btn-default btn-sm">Logout</a>

    </div>
    <section id="entries">
      <h3>Entries</h3>
      <ul class="messages">
      <% for(var i=0; i<messages.length; i++) {%>
        <% if (user.local.email == messages[i].name) { %>
        <li class="message">
          <span class="hide3"><%= messages[i].name %></span>
          <span><%= messages[i].msg %></span>
          <span><%= messages[i].like %></span>
          <span><%= messages[i].thumbUp %></span>
          <span><i class="fas fa-star" aria-hidden="true"></i></span>
          <span class="hide2"><i class="fa fa-thumbs-down" aria-hidden="true"></i></span>
          <span><i class="fa fa-trash" aria-hidden="true"></i></span>
        </li>
      <% } %>
        <% } %>
      </ul>
    </section>
    <div class="row">
         <!-- LOCAL INFORMATION
         <div class="col-sm-6">
            <div class="well">
                <h3><span class="fa fa-user"></span> Local</h3>
                <% if (user.local.email) { %>
                    <p>
                        <strong>id</strong>: <%= user._id %><br>
                        <strong>email</strong>: <%= user.local.email %><br>
                        <strong>password</strong>: <%= user.local.password %>
                    </p>
                    <a href="/unlink/local" class="btn btn-default">Unlink</a>
                <% } else { %>
                    <a href="/connect/local" class="btn btn-default">Connect Local</a>
                <% } %>
            </div>
        </div> -->
        <div class="col-sm-6">
          <h2>Add An Entry</h2>
        <% if (user.local.email) { %>
          <form action="/messages" method="POST">
            <input class="hide" type="text" value="<%= user.local.email %>" name="name">
            <input type="text" placeholder="Date" name="msg">
            <input type="text" placeholder="How Are You Feeling?" name="msg">
            <textarea name="msg" cols="40" rows="5" placeholder="Today Im Feeling... because..."></textarea>
            <button type="submit">Submit</button>
          </form>
        </div>
        <% } %>
        <a href="/save"> <button id="save" type="button" name="button">Go to Favorites</button> </a>
</div>
<script src="main.js"></script>
</body>

</html>
